# This is used by Rockcraft, which expects `app.py` in the project root.
# https://documentation.ubuntu.com/rockcraft/en/stable/reference/extensions/flask-framework/#project-requirements
from webapp.app import app