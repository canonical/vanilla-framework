name: Vanilla framework documentation
version: 2.6.0

# Makes pretty (descriptive) permalinks. See Jekyll docs for alternatives.
permalink: pretty

plugins:
  - jekyll-redirect-from

exclude: ['nginx.conf', 'redirects.map', 'Dockerfile']
