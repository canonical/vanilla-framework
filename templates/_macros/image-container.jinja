{% macro image_container(img_url, width, alt='', aspect_ratio='16/9', is_highlighted=false) -%}
  {% if aspect_ratio == '16/9' %}
    {% set aspect_ratio_class = 'p-image-container--16-9' %}
  {% elif aspect_ratio == 'square' %}
    TODO
  {% else %}
    TODO
  {% endif %}

  <div class="{{ aspect_ratio_class }}{% if is_highlighted == true %} is-highlighted{% endif %}">
    <img class="p-image-container__image" src="{{ img_url }}" width="{{ width }}" alt="{{ alt }}">
  </div>
{%- endmacro %}
